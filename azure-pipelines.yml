# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- none

pool:
  vmImage: windows-latest

stages:
- stage: Invoice
  displayName: 'Invoice Test'
  jobs:
    - job: TestJob
      displayName: 'Invoice Test Job'
      steps:
      ## Node.js install
      - task: NodeTool@0
        inputs:
          versionSpec: '18.x'
        displayName: 'Install Node.js'
      ## Run test
      - script: echo "Running invoice test"
        displayName: 'Invoice Test step'
  
- stage: HomeOffice
  displayName: 'HomeOffice Test'
  jobs:
    - job: TestJob
      displayName: 'HomeOffice Test Job'
      steps:
      ## Node.js install
      - task: NodeTool@0
        inputs:
          versionSpec: '18.x'
        displayName: 'Install Node.js'
      ## Run test
      - script: echo "Running home office test"
        displayName: 'HomeOffice Test step'       


